DATABASE_URL=postgresql://luca_user:luca_dev_2026@localhost:5432/luca_passport
JWT_SECRET=luca_dev_secret_2026_sovereign_health
PORT=5000
NODE_ENV=development


# ── LUCA AI coach (the shared AIProvider seam) ──────────────────────────────
# mode: mock | cloud | anthropic | local
# Defaults to mock so the coach works with zero keys (graceful degradation).
# Switch to cloud/anthropic/local and supply the matching key to enable a real LLM.
LUCA_AI_MODE=mock
ANTHROPIC_API_KEY=
LUCA_AI_BASE_URL=
LUCA_AI_MODEL=
LUCA_AI_API_KEY=
