# Preview-only build mode (vite build --mode preview). Loaded by `npm run build:preview`.
# Enables the labeled Spanish PREVIEW locale on the Preview deployment. Stable/production
# builds use plain `vite build` (production mode) and never load this file → English-only.
VITE_SPANISH_PREVIEW=true
